@model Match


    
    <div class="text-center m-0">
        <h3 >@ViewBag.pl1 против @ViewBag.pl2</h3>
    </div>
        <div class="mt-1 mb-3 mt-0 p-2">
            <h4>Победитель: @ViewBag.winner</h4>
            <h4>@Model.Comment.ToString()</h4>
            <h5>Проведено: @Model.Time.ToString()</h5>
        </div>